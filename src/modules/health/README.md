# OpenAnix - Health Check Module

- prefix: /health/

Check for availability of anixart API and Player parsing

paths:

- `/` - get all statuses
- `/api-proxy` - get only api-proxy status
- `/player-parser` - get only player-parser status
